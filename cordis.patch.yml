# dsh-peekedit bundle layer: registers the peek / peek_edit / peek_write
# file tools. Installed via `dsh plugin --profile <name> add dsh-peekedit`;
# the plugin row references the package by name so Node resolution finds it.
- insert:
    - id: tool-peekedit
      name: dsh-peekedit
